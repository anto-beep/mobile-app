// (tabs)/more.tsx \u2014 thin re-export so the 4th tab routes through expo-router
// to the existing /more screen. Keeping this file separate avoids breaking
// other links to `/more` from header CTAs.
export { default } from '../more';
