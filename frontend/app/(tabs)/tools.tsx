// Tools bottom-tab — surfaces the 8-tool index as a primary destination.
// The actual UI lives in `app/tools/index.tsx` and is unchanged so deep links
// like `/tools/budget-calc` continue to work from anywhere.
export { default } from '../tools/index';
